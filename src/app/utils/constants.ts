const PORT="http://localhost:3000"

export const PROJECT_PORT=`${PORT}/projects`;
export const USER_PORT=`${PORT}/users`
export const CATEGORY_PORT=`${PORT}/category`;
export const TASK_PORT=`${PORT}/tasks`
export const LOGIN_PORT=`${PORT}/login`;
export const REGISTER_PORT=`${PORT}/signup`;
